<form method="post" action="{{ path('app_administratif_delete', {'id': administratif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ administratif.id) }}">
    <button class="btn">Delete</button>
</form>
